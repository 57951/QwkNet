# Site settings
title: QWK.NET
description: QWK.NET library (QWK, REP, QWKE) for .NET applications
theme: jekyll-theme-cayman
baseurl: /QwkNet
url: https://0xe25f.github.io

# Repository information (required for local builds and GitHub links)
repository: 0xe25f/QwkNet

# Build settings
markdown: kramdown
highlighter: rouge

# Plugins
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

# Defaults - tells Jekyll to use the theme's default layout for all markdown files
defaults:
  - scope:
      path: ""
    values:
      layout: default

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - README.md